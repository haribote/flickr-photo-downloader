{
  "name": "flickr-photo-downloader",
  "version": "1.0.0",
  "description": "Download recent photos from Flickr.",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "format": "prettier --write *.mjs **/*.mjs",
    "start": "node -r dotenv/config --experimental_modules index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KIMURA Tetsuro",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^8.0.0"
  },
  "devDependencies": {
    "prettier": "^1.17.1"
  }
}
